/* import City from "../models/City.js"

const update = async(req, res, next) => {
    try {
        let city = req.body
        let uodate = await City.updateOne(
            { _id: city_id }
        )
    } catch (error) {
        next(error);
    }
    }

export default update */